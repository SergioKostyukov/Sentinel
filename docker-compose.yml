services:
  monitoring:
    build:
      context: ./SentinelApi.Monitoring
      dockerfile: SentinelApi.Monitoring/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
